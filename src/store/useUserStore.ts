/**
 * Global user state (Zustand/Redux). To be implemented when auth is added.
 */
// import { create } from 'zustand';
// type UserState = { user: User | null; setUser: (u: User | null) => void };
// export const useUserStore = create<UserState>((set) => ({ ... }));
